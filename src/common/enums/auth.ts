export enum IdentifierType {
	EMAIL = 'email',
	PHONE = 'phone',
	USERNAME = 'username',
}

export enum CookiePrefix {
	Host = '__Host-',
	Secure = '__Secure-',
}

export enum AuthProvider {
	GOOGLE = 'google',
}
